name: DEV-801 - Android

on:
    push:

jobs:
    build:
        name: build
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v5

    tests:
        name: unit tests
        runs-on: ubuntu-latest
        needs: build
        steps:
            - uses: actions/checkout@v5
